# 要点
- 在s01的基础上，多添加几个工具

# 知识点
- 把多个工具串起来，用字典存储对应关系
- **解包

# 踩坑
- 工具被映射的对应需要用lambda对象来存储
- 总共的字符数统计，不能够用int直接做累计存储，因为init、str、tuple都是不可变的。假设对于某个int变量来说，外部赋值，函数内如果做了变量值的修改，不会影响到外部。
但列表/字典支持这种可变，agent_loop里面的history/messages就是可变的，所以可以直接累加。